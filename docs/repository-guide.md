<!-- repository-guide:generated v1; source: EclipseForgeLanding/scripts/repository-profiles.mjs -->
# CineMate — путеводитель по репозиторию

Веб-приложение поиска и просмотра информации о фильмах с OMDb.

[Основной README](<../README.md>) · Поиск фильмов · Личные продукты

## Основной маршрут

1. Поиск.
2. Фильм.
3. Подробности.

Это карта пользовательского сценария, а не отчёт о production-проверке.

<a id="start"></a>

## С чего начать

1. Откройте [README](<../README.md>): в нём остаются подробности установки и эксплуатации.
2. Определите нужный компонент по карте ниже. Для первого запуска используйте отдельную локальную среду и демонстрационные данные.
3. Проверьте зависимости, переменные окружения и стоимость внешних провайдеров. Значения ключей не переносите в README, issue или prompt.
4. Запустите только нужный компонент; миграции, фоновые сервисы и публикация не являются частью обычного просмотра проекта.

<a id="map"></a>

## Карта кода

| Путь | Что искать |
| --- | --- |
| [src/](<../src/>) | Основные исходники |
| [index.html](<../index.html>) | HTML-точка входа |
| [package.json](<../package.json>) | Node manifest и команды |
| [docs/](<../docs/>) | Документация проекта |

<a id="checks"></a>

## Запуск и проверка

Следующие команды **объявлены в manifest**, но не запускались при оформлении репозитория. Выполняйте их из указанной папки, после настройки среды по README.

| Папка | Команда | Источник |
| --- | --- | --- |
| `.` | `npm run dev` | [package.json](<../package.json>) |
| `.` | `npm run build` | [package.json](<../package.json>) |
| `.` | `npm run preview` | [package.json](<../package.json>) |

Перед принятием изменения проверьте: запуск нужного компонента, основной сценарий, ошибочный ввод, восстановление после ошибки и отсутствие секретов в diff. Сборка сама по себе не доказывает работоспособность пользовательского сценария.

<a id="boundaries"></a>

## Границы и права

Каталог зависит от доступности и лимитов внешнего API; ключ не следует публиковать вместе с демонстрацией.

Условия кода описаны в [LICENSE.md](<../LICENSE.md>). Права на сторонние материалы проверяются отдельно; этим оформлением лицензия не менялась.

## Как вносить изменения

Сохраняйте существующие соглашения репозитория. Работайте над одной задачей, прикладывайте проверяемый результат и явно различайте локальную реализацию, тестирование и публикацию. Для AI-разработки в экосистеме применяется Codex; сторонние prompts и материалы не становятся активными правилами автоматически.

Оформление проверено по структуре файлов 2026-09-08. Актуальные версии, release-статус и доступность сервисов проверяются в самом проекте.
